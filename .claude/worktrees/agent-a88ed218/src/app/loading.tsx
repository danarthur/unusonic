import { SystemLoader } from '@/shared/ui/feedback/system-loader';

/**
 * Root loading UI. Next.js shows this during navigation and chunk loading.
 */
export default function Loading() {
  return <SystemLoader />;
}
