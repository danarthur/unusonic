/**
 * Catalog (Master Menu) — Server Component route entry. Interactive surface
 * lives in page-client.tsx.
 */
import CatalogPageClient from './page-client';

export default function CatalogPage() {
  return <CatalogPageClient />;
}
