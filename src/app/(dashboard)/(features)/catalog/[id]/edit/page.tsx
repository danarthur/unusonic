/**
 * Catalog item edit — Server Component route entry. Interactive surface
 * lives in page-client.tsx.
 */
import CatalogEditPageClient from './page-client';

export default function CatalogEditPage() {
  return <CatalogEditPageClient />;
}
