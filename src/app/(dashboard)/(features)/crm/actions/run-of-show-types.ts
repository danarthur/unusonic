/**
 * Re-export canonical RoS types from the features layer.
 * Do NOT re-declare these interfaces here — keep a single source of truth.
 */
export type { Cue, CueType, AssignedCrewEntry, AssignedGearEntry, RosTemplate } from '@/features/run-of-show';
