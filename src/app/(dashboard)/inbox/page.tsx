/**
 * Inbox — Server Component route entry. Interactive surface lives in
 * page-client.tsx.
 */
import InboxPageClient from './page-client';

export default function InboxPage() {
  return <InboxPageClient />;
}
