/**
 * Capture — Server Component route entry. Interactive surface lives in
 * page-client.tsx.
 */
import CapturePageClient from './page-client';

export default function CapturePage() {
  return <CapturePageClient />;
}
