/**
 * Auth entity — public surface.
 * Cross-feature types for the three-track auth redesign.
 */

export type {
  Role,
  InvitationSummary,
  DeviceCapability,
  AuthContinueResolution,
} from './model/types';
